#name: Poll Chariot PRs and Generate Doc Suggestions

#on:
#  schedule:
#    # Run daily at 09:00 UTC (adjust as needed)
#    - cron: '0 9 * * *'
#  workflow_dispatch:
#    inputs:
#      dry-run:
#        description: 'Dry run — log what would happen without creating PRs'
#        required: false
#        default: 'false'
#        type: boolean

#permissions:
#  contents: write
#  pull-requests: write

# concurrency:
#   group: poll-chariot-prs
#   cancel-in-progress: false

# jobs:
#   poll:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4
#         with:
#           # Need full history to commit state file updates
#           fetch-depth: 0

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Claude Code CLI
#         run: npm install -g @anthropic-ai/claude-code

#       - name: Poll chariot for merged PRs
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           CHARIOT_REPO: praetorian-inc/chariot
#           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
#           DRY_RUN: ${{ github.event.inputs.dry-run || 'false' }}
#         run: npm run poll

#       - name: Commit updated state file
#         if: github.event.inputs.dry-run != 'true'
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git add processed-prs.json
#           if git diff --staged --quiet; then
#             echo "No state changes to commit"
#           else
#             git commit -m "chore: update processed-prs.json [skip-sync]"
#             git push origin main
#           fi
